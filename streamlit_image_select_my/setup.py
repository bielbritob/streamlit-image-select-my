from setuptools import setup, find_packages

setup(
    name="streamlit-image-select-my",  # Nome ÚNICO para evitar conflitos!
    version="1.0.0",  # Altere conforme sua versão
    packages=find_packages(),
    install_requires=["streamlit"],  # Adicione outras dependências se necessário
)